# 変更履歴

PocketRT の変更を記録します。バージョンは [セマンティック バージョニング](https://semver.org/lang/ja/) に従います。

各バージョンの見出しには、TestFlight に配布したビルド番号を併記します。配布していない変更は「未リリース」に置きます。

---

## 1.0.0 — 2026-08-21（build 10）

初回の一般公開。0.9.x 系を TestFlight で配布したのち、下記を積んで 1.0.0 とした。

### 追加

- **自施設の判定基準にも書き出しと読み込みを追加した。** プリセット（D1）と同じく、ネットワークは介さない。読み込みは既存を消さず、id が一致するものだけ上書きする。取り込む前に件数と扱いを確認するダイアログを出す。文言には「自施設の基準として扱われ、**判定に使われます**」を含めた。プリセットは計算の入力値だが、判定基準は「基準内 / 基準を超える」という臨床的な言明を直接生むため、他施設の基準を取り込んだことを忘れたまま使う害が大きい
- 保存データが壊れて読めないとき、**破棄するほかに、書き出しておいたファイルから復旧できるようにした。** 破棄は登録を失うが、読み込みは取り戻せる。編集画面では読み込みを先に出す
- **保存データが「無事なまま読めない」ときは読み込ませないようにした。** 端末のロックや I/O 障害で開けないだけの場合と、保存データが新しい形式で読めない場合の 2 つは、アプリを再起動するか更新すれば読める。ここで読み込んで置き換えると、無事なデータを利用者自身の手で失うことになる。どちらの状態でも導線を出さず、先に取るべき手段（再読み込み、アプリの更新）を伝える

- **自施設のプロトコルを登録できるようにした。** 施設ごとの線量分割をプリセットとして保存し、計算・予定・合算の各タブで選べる。α/β は任意で、空欄にすると選んでも α/β を変更しない。プロトコルが α/β を定めていないのに値を入れさせると、存在しない臨床的判断をアプリが作り出すことになるため
- 内蔵プリセットは一覧での表示を切り替えられるようにした。**値の編集はできない。** 内蔵は出典と一体で意味を持ち、線量を書き換えると出典を表示したまま中身が別のものになるため
- 自施設プリセットの書き出しと読み込みを追加した。ネットワークは介さない。読み込みは既存を消さず、id が一致するものだけ上書きする
- **品質タブの逸脱判定に、自施設独自の基準を登録できるようにした。** R100% / R50% / D2cm のそれぞれに、基準内・許容の 2 段階（許容は空欄にでき、その場合は基準内か超過かの 2 段階になる）で閾値を設定できる。PTV 体積の表に載っていない症例や、公表プロトコルが検討していない線量分割でも判定できるようにするため
- 自施設の基準を選んでいる間は、段階名を「基準内 / 基準をやや超える / 基準を超える」にした。「Per protocol」は公表プロトコルへの適合を指す語であり、利用者が自分で登録した基準にこの語を出すのは意味の誤りになるため
- 自施設の基準を選んでいる間、判定パネルに「この判定は利用者が登録した基準によるものです。公表されたプロトコルへの適合を示すものではありません」を常時表示するようにした。公表プロトコル用の文言とは同時に出ない。判定が出るのに根拠の説明が無い状態を作らないため
- 出典一覧の「品質指標の逸脱判定」節に、自施設の基準を小節として追加した。各件に「出典なし。利用者が登録」を明示する。この画面はアプリが何を根拠にしているかを確かめる場所であり、隠すと実際に使える判定の一部が点検対象から外れるため
- 出典の状態を 3 段階（一次文献 / ガイドライン / 根拠不明）で表せるようにした。ガイドラインは版と章を書けば読者が確かめられる出典であり、根拠を同定できていない状態とは区別する。この変更にあわせて、出典未特定としていた項目を JASTRO 放射線治療計画ガイドライン 2024 と PubMed で照合した結果を反映し、**根拠が同定できていない出典は 0 件になった**
- 自施設プリセットは出典を持ちえないため、計算結果には出典ではなく「自施設のプロトコル: 名前」を別の欄に出す。出典の欄に名前を出すと、名前が出典であるかのように読めるため
- プリセットの出典一覧に「自施設のプロトコル」節を設け、各件に「出典なし。利用者が登録」を明示した。この画面はアプリが何を根拠にしているかを確かめる場所であり、自施設プリセットを隠すと実際に選べるレジメンの一部が点検対象から外れる

- プライバシーポリシーをアプリ内で読めるようにした。情報画面から開く。外部サイトへのリンクは廃止した

- **品質タブに頭部定位照射（SRS/SRT）の逸脱判定を追加した。** RTOG radiosurgery QA guidelines（Shaw E, et al. 1993）の基準で、Homogeneity index（MDPD）と Conformity index（PITV）の 2 指標を判定する。特定の線量分割にも PTV 体積の範囲にも依存しない（肺 SBRT の判定表とは対象が異なるため）
- 頭部定位照射を選んでいる間、判定パネルに「原典は Coverage・MDPD・PITV の 3 基準を定めているが、本アプリは MDPD と PITV の 2 つだけを判定している」旨を常時表示するようにした。黙って 2 基準だけ判定すると、3 基準すべてを満たしたと利用者が誤解しうるため
- 頭部定位照射を選んでいる間、指標カードの GI (Paddick) に「標的が近接して複数ある場合、この値は意味をなさないことがあります」を添えるようにした。多発脳転移は頭部定位照射の主要な適応であり、病変が近接する症例では GI が意味をなさないことがあるため。肺 SBRT を選んでいる間や未選択のときは出さない
- 頭部定位照射の判定パネルに、原典が明示していない境界を安全側（重い方の判定段階）に倒していることと、1993 年発表の文書であること（IMRT / VMAT や多発病変への単一アイソセンター照射が普及する前のもの）を明示した

### 変更

- **線量分割プリセットの採用基準を変更した。** 従来は一次文献の裏付けがあることを基準にしていたが、JASTRO 放射線治療計画ガイドライン 2024 に明記されていることを基準に改めた。日本の放射線治療医と医学物理士を広く対象とする以上、十分なコンセンサスが得られているとは言い難い線量分割を一覧に並べるのは対象設定と合わないため。全 23 件をガイドラインと照合し、次の 5 件を削除した（23 → 18 件）

  | プリセット | 削除理由 |
  |---|---|
  | 脳転移 分割SRT 27 Gy/3 Fr、30 Gy/5 Fr | ガイドラインは 28〜32 Gy/4 分割とし、分割数が一致しない。同章は「適応と至適線量についてのエビデンスは少ない」とも記載 |
  | 脊椎転移SBRT 24 Gy/2 Fr | ガイドラインは 24 Gy/3 回とし分割数が一致しない。かつその記載は再照射の文脈で適応が異なる |
  | 前立腺SBRT 36.25 Gy/5 Fr | 具体的線量の記載がなく、経験のある施設で慎重にすべきとされている |
  | 肝SBRT 40 Gy/5 Fr | 報告例の列挙のみで、単一の標準が示されていない |

  乳癌超寡分割（FAST-Forward 26 Gy/5 Fr）は 2024 年版に超寡分割照射として記載があり、採用基準を満たす。アプリが出典に挙げているのは一次文献（Murray Brunt 2020, Lancet）で、出典の格としてはガイドラインより強い

- 骨転移の 3 件の名前を「骨転移」に統一した。「シングル」「中等回数」「マルチ」という呼び分けは一般的でなく、線量と分割の併記で区別できる
- カレンダーの日の種別（治療日、祝日、個別休止、週末、期間外）を1箇所で判定し、その結果を色と記号と VoiceOver の読み上げに使うようにした
- 色だけで区別していた日に記号（祝、休）を加えた。色覚特性やモノクロ表示でも区別できる
- 祝日を赤、個別休止をオレンジに変更した。日本の暦の慣習に合わせた
- カレンダーの表示月数を治療期間から導出するようにした。以前は治療が1ヶ月で終わる場合も3ヶ月分を表示していた
- 数値入力欄の固定幅ラベルを廃し、ラベルを入力欄に関連付けた。文字サイズを大きくしてもレイアウトが崩れない
- 判定の許容値を等幅数字で表示し、桁が揃うようにした

- 判定パネルの注記を 2 層にした。**判定結果の読み方を変えるもの**（何を判定していないか、表にない体積を補間していること、利用者定義の基準によることなど）は常時表示し、**プロトコルの背景や方法論**（原典が定めていない境界の扱い、判定表の適用範囲、指標の定義差）は「この判定基準について」に畳んだ。根拠を必ず示す方針で注記が積み上がり、判定そのものより場所を取っていたため
- 判定が 1 つも出ていないときのカードの見た目を、どのプロトコルでも同じにした。以前は肺 SBRT と頭部定位照射で分かれていた。同じ「まだ何も判定していない」状態が別の見た目になっていると、状態の意味が読み取れないため
- 閾値を定めていない指標を「未設定」と表示し、「基準内」と見分けられるようにした。判定していないことと基準を満たしていることは違うため
### 修正

- **合算タブの α/β をコースごとではなく画面に 1 つにした。** BED は組織ごとの量で、α/β を選ぶ行為が「どの組織の生物効果を見ているか」を決めている。コースごとに別の α/β を入れられると、腫瘍の BED と晩期反応組織の BED を足すような操作ができ、**その和が生物効果を表す組織は存在しない**（摂氏と華氏を足すのと同じ）。合算が意味を持つのは評価対象の組織を 1 つ決めたときだけなので、α/β はコースではなく評価対象に属する

  従来も α/β が食い違うと `Σ BED` の添字（Gy₁₀ の 10）を落として表示していたが、**数字そのものは出ていた。** 添字が消えたことに気づかなければそのまま読まれる。画面に 1 つに縛ることで、混ざった和を作れなくした。添字も常に出る

  `Course` 型からも `alphaBeta` を外し、`LQCore.cumulativeBED(courses:alphaBeta:)` が α/β を 1 つだけ受け取る形にした。呼び出し側の注意ではなく、型で表現できないようにしてある

  プリセットをコースに当てても、画面の α/β は変えない。プリセットが持つ α/β はその部位の代表値であって、いま評価している組織とは別物である。コースへの操作で他のコースの評価基準まで黙って変わるのを避けた。α/β は上の欄で明示的に選ぶ（α/β プリセットもそこにある）

- **数値入力欄の並びを整えた。** 3 点あった。(1) ラベルの長さがそのまま入力欄の開始位置になるため、欄ごとに左端がばらついていた。「PTV 体積」「PIV」「D2%」が縦に並ぶ品質タブでは 11 個の欄がすべて別の位置から始まり、目が拾う手がかりを失っていた。(2) 単位（cc / Gy / Fr）の文字幅の違いが、そのまま入力欄の右端のずれになっていた。(3) 入力欄が行の余白をすべて吸って画面幅いっぱいに広がっていた。ここに入るのは 2〜4 桁の数値で、その桁数に対して広すぎる欄は入力できる量を過大に見せる

  **ラベルを行の左端、入力欄と単位を右端に置く構成にした。** 入力欄の幅を固定してあるので、右端から数えた位置が行によらず、左端も右端も全行で揃う。ラベルの長さは揃いに影響しない。セクション見出しとも左端が揃う

  **固定幅ラベルには戻していない。** それは文字サイズを大きくするとラベルが切れるため 2026-08-04 に撤去した経緯がある。**アクセシビリティ用の文字サイズでは、ラベルを上・入力欄を下に積む構成に切り替える。** 横に並べたままだと行が画面幅を超え、ラベルが左から切れる。実際に最大の文字サイズで撮って確認した

- **数値キーパッドを閉じられなかった。** `.decimalPad` にはリターンキーが無く、SwiftUI は画面の余白を叩いても閉じない。アプリ側にも「完了」ボタンやスクロールで閉じる仕掛けを置いていなかったため、入力欄を一度叩くと**キーボードが覆っているタブバーに触れられず、タブを切り替えられなくなっていた**。数値入力のある 5 画面すべてに、キーボード上の「完了」とスクロールによる却下を入れた。片方だけでは足りない——ツールバーだけだとスクロールしても閉じないことに戸惑い、スクロールだけだと内容が短い画面で閉じる手段が無くなる。**UI テストを書いて初めて分かった。**人手の確認では、閉じられないことに気づいても「そういうもの」として流してしまう

- **書き出したファイルを自分で読み込めなかった**（自施設プリセット）。共有シートに `Data` をそのまま渡していたため、書き出したファイルが JSON を名乗らず、拡張子も付かない。読み込みは JSON だけを一覧に出すので、書き出したファイルが選択肢に現れなかった。書き出しと読み込みは対で意味を持つ機能であり、片道では用をなさない。content type と拡張子を明示する型を挟んで直した。**この経路は手動確認の項目が未消化のまま残っており、誰も往復を試していなかった。**同じ形を D2 に持ち込まないよう、content type を固定する自動テストを置いた

- 保存データが壊れて読めない場合に、それを「未登録」と解釈して上書きし、利用者の登録を失う経路があった。ファイルが存在して読めない場合は必ずエラーとして扱い、読み込みに失敗している間は保存しないようにした。失敗していることは画面に出し、追加・編集・削除の導線を無効にする。黙って効かない状態にしない
- 書き出しておいたファイルの読み込みが、壊れた保存データからの復旧手段として働くようにした。読み込みを禁止すると、利用者が行き止まりから抜けられない
- 自施設プリセットの削除に確認を挟むようにした。手で入力して長く使うデータであり、取り消す手段がない
- **文字列抽出がビルドで動いていなかった。** ビルド設定 `SWIFT_EMIT_LOC_STRINGS` の既定は `NO` で、無効のままでは正しく書いた `Text("リテラル")` すら String Catalog に入らない。カタログは文章監査より前の 53 件で凍結しており、修正した文言の旧版が残っていた。抽出を有効にし、`tools/sync-strings.sh` で同期できるようにしたうえで、CI が鮮度を検査するようにした
- 解説の見出しと本文、プリセット名と注記、α/β ラベルと注記、出典注記が素の `String` で保持されており、String Catalog に載っていなかった。`LocalizedStringResource` に変えた
- 数値入力欄のラベルと単位、結果カードの見出し、プライバシーポリシーの節見出し、個別指定の見出し、祝日名も同じ状態だった。素の `String` パラメータに渡したリテラルは抽出されず、同じ文字列が他所のローカライズ経路にある場合だけ偶然カタログに現れていたため、カタログを見ても気づけなかった
- 品質タブの許容値を示す行は書式文字列を `String(format:)` に直接渡しており、カタログに一度も載っていなかった
- 出典未特定としていたプリセットのうち 2 件で一次文献を確定した。Glioblastoma 60 Gy/30 Fr は Stupp R, et al. *N Engl J Med.* 2005（PMID 15758009）、骨転移 8 Gy/1 Fr は Bone Pain Trial Working Party. *Radiother Oncol.* 1999（PMID 10577696）。いずれも要旨で線量と分割まで確認した

  カタログは 53 件から 249 件になった。表示は変わらない。英訳の投入は行っていない

- カレンダーの記号と凡例が String Catalog に登録されず、英語環境で翻訳されない状態だった。`Text` が `String` を受け取る初期化子に解決されていたことが原因で、`LocalizedStringKey` を返すよう修正した
- 解説文の「治療期間の延長」の記述を3段階に書き分けた。計画的な短縮が転帰を改善することはランダム化試験で支持され、予期せぬ中断による延長の影響は観察研究にとどまり、補正によって転帰を回収できる臨床エビデンスは確認できていない。以前の記述はこの3つを区別していなかった
- 均一性指数（HI）の解説で、RTOG 型と ICRU-83 型それぞれの下限（1 と 0）を明示した
- 「物理士」を「医学物理士」に統一した
- **範囲外の値を含むファイルを読み込むとアプリが復旧不能になる経路があった。** 読み込みは形式だけを検査していて値の範囲を見ていなかったため、線量として扱えない大きな値がそのまま保存されえた。その値は一覧の表示時に整数へ変換されるところで実行時エラーになり、編集画面も同じ表示を含むため、**問題の項目を削除する手段が失われた。** 読み込み時に全件を検証し、1 件でも範囲外なら取り込まないようにした。部分的に取り込むと、利用者が何が入って何が入らなかったかを確認できないため。あわせて、線量と α/β の表示を整形する処理を 1 箇所にまとめ、変換できない値では小数表記に落ちるようにした
- 保存データが権限や入出力の障害で読み出せない場合を「ファイルがない」と誤認していた。ファイルの有無と読み出しの可否を分けて判定し、読めない場合は読み込み失敗として扱うようにした。原因が違えば利用者が取れる対処も違うため、壊れている場合とは文言を分けている
- 保存先が用意できない場合に、一時的な場所へ黙って書き込んでいた。利用者には登録がないように見え、その領域が消えれば登録も消える。フォールバックを廃し、保存できないことを画面に出すようにした
- プリセットを選ぶ画面が読み込み失敗を伝えておらず、「まだ登録していない」のか「読めていない」のかを区別できなかった。管理画面と出典一覧には出ていた警告が、日常的にいちばん開くこの画面だけ抜けていた
- 保存先が用意できない状態で読み込むと、成功したと表示されるのに何も保存されず、次回起動で登録が失われる経路があった。読み込みで解除してよいのは「保存データを読めなかった」状態だけで、「保存先がない」状態は解除してはならない。この 2 つを区別するようにした
- 読み込みの前に、何件を取り込むのか、既存に合流するのか置き換えるのか、そして読み込んだ内容が自施設のプロトコルとして扱われることを確認する画面を挟むようにした
- 予定タブの「強制照射日」「強制休止日」の見出しに、文字列の内部表現がそのまま表示されていた。ローカライズ対応で型を変えた際、文字列を組み立てている箇所が追随していなかった。型は正しく、ビルドも通り、文言も翻訳対象として登録されるため、テストでもコードレビューでも検出できず、実機表示の確認だけが捕まえた
- 換算の内部処理で、極端な入力に対して実行時エラーになりうる整数変換が残っていた。呼び出し側の入力検証だけがこれを防いでいる状態だったため、処理そのものが範囲外の入力を受け取っても落ちないようにした。臨床的な上限は設けていない
- **逸脱判定の根拠がアプリ内から確認できなかった。** 判定は「Per protocol」「Minor deviation」「Major deviation」という、線量分割プリセットの表示より強い臨床的な言明をする。それにもかかわらず画面に出るのは試験番号だけで、出典一覧にも載っていなかった。両試験を出典として登録し、出典一覧に「品質指標の逸脱判定」の節を設けた。判定に使う表は公表論文ではなく各試験のプロトコル文書にあるため、論文が表そのものを含まないことも明記している
- 判定表がどちらの群に対する基準なのかが読み取れなかった。表は体積の比と処方線量に対する割合だけで定義されており絶対線量を含まないため、群や線量レベルによらず共通である。この点を明示した
- **判定の基準となる指標の定義が、原典間で食い違っている件を開示した。** 一方の試験は 50% 等線量体積で定義し、他方は本文で別の線量の等線量体積と記している。本アプリがどちらに従って計算しているかを示した。従来は説明がなく、原典の記述どおりに計算した利用者と値が食い違いえた
- **試験が検討していない線量分割を「プロトコルに一致」と判定していた。** 分割数の範囲だけを見て 1 回線量を見ていなかったため、どの群にも存在しない組み合わせが判定を通っていた。検討された線量分割の組そのものと照合するようにした。一致しない場合は、何が検討されたのかと入力値の両方を示す
- 原典の表記ゆれを補正して転記していることを、アプリ内でも示すようにした。黙って直すと、原典と突き合わせた利用者が食い違いに戸惑う

---

- **自施設の基準を選んだ状態でその基準を削除しても、判定が消えずに出続けていた。** 選択が基準の値そのものを控えていたため、一覧から消えても判定だけが残った。同じ理由で、選択中の基準を編集しても判定が古い閾値のままだった。選択は基準を指す識別子だけを持ち、判定のたびに引き直すようにした。削除された基準を選んでいた場合は、黙って「判定しない」に戻さず、削除された旨を表示して選び直しを促す
- 肺 SBRT の判定表についての説明が、頭部定位照射を選んでいるときにも出ていた。頭部定位照射は別の指標で判定し、線量分割という概念も持たないため、この説明は当てはまらない
- 頭部定位照射の許容値表示が、実際の判定と異なる不等号になっていた。原典は均一性指数を「2.0 以下なら逸脱なし」と明示しており判定もそのとおり動いていたが、表示だけが「2.0 未満」と読める状態だった。判定は正しく出るのに根拠の表示が食い違うと、利用者は正しい判定のほうを疑うことになるため

## 0.9.1（2026-08-04 / TestFlight build 2）

### 追加

- **品質タブ**を新設した。SBRT と SRS のプラン品質指標を算出する。適合性指数は RTOG 型（R100% と同値）と Paddick 型、均一性指数は RTOG 型と ICRU-83 型、これに R50%、勾配指数（Paddick 型）、D2cm を加えた7つを表示する
- 品質タブに RTOG 0813 / 0915 の適合性判定表を組み込んだ。PTV 体積から許容値を線形補間し、逸脱なし / minor / major の3段階で判定する。PTV 体積が表の範囲（1.8〜163.0 cm³）を外れる場合は、外挿せず判定を出さない
- 予定タブに治療期間全体を見る**カレンダー**を追加した。治療日、祝日、個別の休止日を色分けして表示する
- 線量分割プリセットに**一次文献の書誌情報**を紐づけた。1つのプリセットが複数の出典を持てる。出典を特定できなかったプリセットは、推測で埋めず注記を付けた
- 計算結果にプリセットの出典を併記するようにした
- **情報画面**を新設し、各タブの右上の ⓘ から開けるようにした。計算式、α/β 値の根拠、プリセットの出典、免責事項を収録した
- String Catalog を導入し、英語ローカライズの基盤を整えた

### 変更

- 出典の追跡可能性を記録した `docs/data-sources.md` を公開した。RTOG 0813 と 0915 の Table 1 が誤植を含めて同一であること、その誤植をどう補正したか、その根拠を記録している

### 修正

- 線量分割プリセットの出典を検証した過程で、JCOG0702 としていた文献が JROSG10-1 の誤りであることが判明したため訂正した

---

## 0.9.0（2026-07-31 / TestFlight build 1）

TestFlight 内部テストで開発者本人の iPhone に配布した最初のビルドです。

### 追加

- アプリアイコンを追加した
- TestFlight 配布用のビルド設定を追加した
- プライバシーポリシーを公開した

### 変更

- アプリ名を PocketRT に変更した
- iPhone 専用に指定した
- **「OAR制約換算」を「線量分割換算」に改めた。** 同じ式は正常組織の制約に限らず腫瘍の処方にも使えるため、名称が用途を狭めていた
- 線量分割換算で、**元の線量分割の BED と EQD2 を換算後の入力の上に表示**するようにした。換算は BED を保つように行うため、上下を見比べれば等価であることを確かめられる。1 回線量を指定した場合は分割数を丸めるため厳密には一致しないが、そのずれも数値で見えるようになった
- 品質指標の逸脱判定で、**プロトコルの選択肢に対象部位と検討された線量分割を併記**するようにした。試験番号だけでは、その基準が自分の症例に当てはまるかを選ぶ時点で判断できなかった
- プロトコルを選ぶと、**何を調べた試験なのか**（対象集団と線量分割）がその場に出るようにした

### 修正

- `Info.plist` のバージョンをビルド設定から参照するようにした。以前は二重管理になっていた
- 予定タブの開始日の初期値が、テストで注入した時計を使わずシステム時刻を読んでいた

---

## 0.1.0 以前

独立リポジトリとして再出発する前の実装です。詳細な履歴はアーカイブ済みのモノレポにあります。

- **計算タブ**: BED と EQD2 の算出、線量分割プリセットの適用、計算式の表示
- **合算タブ**: 最大3コースの BED と EQD2 の累積、再照射時の注意喚起
- **換算タブ**: OAR 制約の分割数換算（分割数指定と1回線量指定の2モード）
- **予定タブ**: 開始日から終了予定日、進捗、直近のスケジュールを算出。日本の祝日に対応
- 初回起動時の免責事項の同意画面
